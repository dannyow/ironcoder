{
    IBClasses = (
        {
            ACTIONS = {continue = id; quit = id; randomPicture = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {myView = MyCIView; warning = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = MyCIView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}